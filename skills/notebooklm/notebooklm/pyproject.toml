[project]
name = "notebooklm"
version = "0.1.0"
description = "NotebookLM integration skills for Beam Next"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28.0",
]

[tool.uv]
dev-dependencies = []
